expr {=
def Allow {=
	def.kv identity data;
	def.body {=
		return true;
	}
};
def CheckNewUser {=
	def.kv identity data;
	def.body {=
		return {=has identity.data 'touristNum'} and {=empty identity.data.touristNum};
	}
}
}